[
    {
        "path": "/users",
        "model": {
            "id": "{{random.number}}",
            "name": {
                "first": "{{name.firstName}}",
                "last": "{{name.lastName}}"
            },
            "address": {
                "city": "{{address.city}}",
                "zipCode": "{{address.zipCode}}",
                "street": "{{address.streetAddress}}",
                "secondary": "{{address.secondaryAddress}}"
            },
            "job": {
                "company": "{{company.companyName}}",
                "title": "{{name.title}}",
                "type": "{{name.jobType}}"
            },
            "emails": [{
                "addresses": ["{{internet.email}}"]
            }, 3],
            "phone": "{{phone.phoneNumberFormat}}"
        }
    }
]